require "spec_helper"

RSpec.describe RestaurantCrawler do
  it "has a version number" do
    expect(RestaurantCrawler::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
